import React from "react";
import Display from "../components/display";

const Home = (props) => {
    return (
        <div className="flex flex-col w-full">
            <Display />
        </div>
        
    );
}

export default Home;